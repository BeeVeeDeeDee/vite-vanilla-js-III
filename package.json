{
  "name": "vite-vitest-crash",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"cd backend && npm run mockend\"",
    "build": "vite build",
    "preview": "vite preview",
    "mockend": "npx json-server db.json --port 3100"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^6.5.1",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
